def validate(email):
    if email.count('@')==1 and all(c.islower() or c.isdigit() or c in "._@" for c in email):
        print("Valid email address")
    else:
        print("Invalid email address")
email=input("Enter email address to validate: ")
validate(email)
