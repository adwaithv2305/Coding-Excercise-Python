def isperfect(n):
    divsum=0
    for i in range(1,n):
        if n%i==0:
            divsum+=i
    if divsum==n:
        return "Perfect number"
    else:
        return "Not a perfect number"
n=int(input("Enter the number: "))
y=isperfect(n)
print(y)
